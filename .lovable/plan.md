Plan: apply the two approved de-personalization rewrites to the `skills` table.

1. **Verify current state** — query the `overview` and `description` fields for `Deal Screen` and `Deal Analyzer & Underwriter` to confirm the target phrases are still present exactly as listed.

2. **Update `Deal Screen` — `overview`**  
   Replace:  
   "That's the difference between a gut-check from someone who's run deals and a chatbot telling you what you want to hear."  
   With:  
   "That's the difference between math built to protect capital and a chatbot telling you what you want to hear."

3. **Update `Deal Analyzer & Underwriter` — `description`**  
   Replace:  
   "…from the perspective of an operator who reviews deals before money goes in."  
   With:  
   "…underwritten the way a deal gets reviewed before money goes in."

4. **Confirm** — re-query both rows to verify the replacements landed and no other fields were touched.

No other skill rows or fields will be modified.
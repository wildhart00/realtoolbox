Add the following meta tag to the `<head>` section of `index.html`, anywhere alongside the other `<meta>` tags:

```html
<meta name="impact-site-verification" value="0d300971-d44d-4b02-a04f-8ca2f3c29b94">
```

No other files change.